# sql-bookstore-project
A relational database project for a bookstore system built using SQL — includes table creation, data relationships, and 19 queries ranging from basic filters to advanced joins and aggregations.
